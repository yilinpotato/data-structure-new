# 新能源物流车队协同调度项目 TODO

## 当前已完成内容盘点

- [x] 项目基础结构已具备：`app.py` Web 入口、`main.py` 命令行入口、模型层、算法层、地图数据层、可视化层。
- [x] 使用图结构表示道路网络：`data/guangzhou_map.py` 中用 NetworkX `DiGraph` 建模广州道路节点、道路边、边权距离。
- [x] 实现寻路算法：`algorithms/path_planning.py` 包含 A* 搜索，并在失败时回退到 NetworkX Dijkstra 最短路径。
- [x] 实现车辆模型：包含有限车辆数、载重上限、电量上限、当前位置、状态、当前任务、历史统计等。
- [x] 实现动态任务生成：任务包含生成时间、地点节点、随机货物重量、可选截止时间。
- [x] 实现评分机制：任务完成越早、路径越短评分越高，失败任务扣分。
- [x] 实现充电站模型：包含充电桩容量、排队队列、占用状态、充电速率、等待时间和利用率统计。
- [x] 实现至少两种调度策略：最近任务优先、最大任务优先；另有紧急任务优先和平衡策略。
- [x] 实现多车协同分配雏形：`cooperative_task_allocation` 可将超出单车载重的大型任务分给多辆车。
- [x] 支持策略对比：`main.py --compare-strategies` 可输出 CSV 和对比图。
- [x] 支持图形界面展示：Flask + 高德地图 Web 页面，包含车辆、任务、充电站、路线和实时统计。
- [x] 支持离线可视化：`visualization/map_visualizer.py` 可生成 Folium 地图和动画 HTML。
- [x] 已安装依赖并做过 smoke test：`compileall` 通过，命令行短模拟和 Web 使用的根目录模拟器短模拟可运行。

## 必做项对照检查

- [x] 1. 有限车辆数、电量上限、载重上限、动态任务模拟。
- [x] 2. 任务包含产生时间、地点坐标或节点、货物重量随机生成。
- [x] 3. 有评分机制，考虑完成时间、路径距离、失败扣分。
- [x] 4. 电量不足时能寻找充电站补能。
- [x] 5. 充电站支持容量、排队和负荷统计。
- [x] 6. 可选择多车协同完成同一任务，已有基础实现。
- [x] 7. 至少两种调度策略，已实现 4 种。
- [ ] 8. 至少模拟三种以上不同规模的问题，需要补充固定实验脚本和结果表。
- [x] 9. 图形界面展示，已有 Web 页面和 Folium 输出。

## 需要优先完善的问题

- [ ] 统一两套模拟器口径：当前 `app.py` 使用根目录 `simulator.py`，`main.py` 使用 `models/simulator.py`。建议保留一套核心模拟逻辑，Web 和 CLI 都调用同一个模拟器，避免报告和展示结果不一致。
- [ ] 修正并统一距离/电量单位：部分代码中距离是米，车辆电量按 km 表示，`models/vehicle.py` 的 `can_reach(distance)` 直接比较可能导致逻辑不严谨。建议统一为米或 km，并在函数名/注释中写清楚。
- [ ] 验证充电流程闭环：`manage_charging` 目前规划去充电站路径后，是否到站后真正进入排队/充电需要完整测试；充电站里也存在状态字符串和 `VehicleStatus` 枚举混用的问题。
- [ ] 强化多车协同任务完成逻辑：目前能分配协同车辆，但需要确认多辆车到达后如何共同完成、如何计算任务距离、时间和分数。
- [ ] 修复中文控制台乱码：Windows PowerShell 下运行 `main.py` 输出为乱码，建议统一设置 UTF-8 输出或在 README 中说明运行前执行 `chcp 65001`。
- [ ] 清理高德地图 Key：`templates/index.html` 中写死了 API Key 和 securityJsCode，建议改为配置文件或环境变量，避免提交敏感信息。
- [ ] 补充仓库清理确认：当前 `.gitignore` 已有基础规则，但需要确认运行产物、缓存和敏感配置不会进入最终提交。

## 实验与报告 TODO

- [ ] 设计三种规模实验：
  - 小规模：5 辆车、1 小时、低任务率。
  - 中规模：10 辆车、4 小时、中任务率。
  - 大规模：20 辆车、8 小时、高任务率。
- [ ] 每种规模至少运行最近任务优先、最大任务优先、紧急任务优先、平衡策略各 3 次，保存随机种子、参数和结果。
- [ ] 生成结果表：总任务数、完成率、失败数、总评分、平均完成时间、平均路径距离、车辆利用率、充电站利用率、平均排队时间。
- [ ] 生成图表：不同策略完成率对比、总评分对比、平均任务时间对比、车辆利用率对比、充电站负荷对比。
- [ ] 写报告的“模型设计”部分：说明图结构、节点/边/权重、车辆状态机、任务状态机、充电站队列模型。
- [ ] 写报告的“算法设计”部分：说明 A*/Dijkstra、最近任务优先、最大任务优先、紧急任务优先、平衡策略、多车协同策略。
- [ ] 写报告的“实验分析”部分：解释不同规模和不同策略下的结果差异。
- [ ] 准备展示视频脚本：项目背景、系统启动、参数设置、车辆调度动画、策略对比结果、总结亮点。

## 可选加分方向

- [ ] 增加静态全局最优基线：在某一时间段任务全已知时，用整数规划建模并接入 Gurobi/CPLEX，和动态策略做差距分析。
- [ ] 增加元启发式算法：例如遗传算法、模拟退火或蚁群算法，用于批量任务路径规划。
- [ ] 增加强化学习或多智能体方法：把车辆视为智能体，状态包含位置、电量、载重、任务池和充电站负荷。
- [ ] 接入真实交通权重：道路边权加入拥堵系数，让同一路径在不同时段成本不同。
- [ ] 增加多仓库场景：扩展为多个仓库、多车队协同调度。
- [ ] 增加测试用例：为路径规划、任务评分、充电排队、策略选择、多车协同分别写单元测试。

## 最近下一步建议

1. 先统一核心模拟器，修正单位和充电状态问题。
2. 写一个 `experiments.py`，自动跑三种规模和多策略对比。
3. 输出稳定的 CSV/PNG 结果，直接服务报告和展示视频。
4. 清理仓库：确认 `.gitignore` 生效，移除运行产物和敏感 Key。
